<UserControl x:Class="VTE.WPF.Controls.SearchableRelationPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search box + selected display -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch" Grid.Column="0"
                     Padding="8,6" FontSize="13"
                     KeyDown="TxtSearch_KeyDown"
                     TextChanged="TxtSearch_TextChanged"/>
            <Button x:Name="BtnSearch" Grid.Column="1" Content="&#x1F50D;"
                    Click="BtnSearch_Click" Padding="8,6" Margin="4,0,0,0"
                    Background="{DynamicResource SurfaceBg}"
                    Foreground="{DynamicResource TextSecondary}"
                    BorderThickness="0" FontSize="13" Cursor="Hand"/>
            <Button x:Name="BtnClear" Grid.Column="2" Content="&#x2715;"
                    Click="BtnClear_Click" Padding="8,6" Margin="4,0,0,0"
                    Background="{DynamicResource SurfaceBg}"
                    Foreground="{DynamicResource TextMuted}"
                    BorderThickness="0" FontSize="13" Cursor="Hand"/>
        </Grid>

        <!-- Results popup -->
        <Border x:Name="ResultsPanel" Grid.Row="1"
                Background="{DynamicResource CardBg}"
                BorderBrush="{DynamicResource InputBorder}"
                BorderThickness="1" CornerRadius="4"
                MaxHeight="250" Visibility="Collapsed" Margin="0,2,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ResultsList"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
