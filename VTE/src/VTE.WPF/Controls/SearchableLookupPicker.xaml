<UserControl x:Class="VTE.WPF.Controls.SearchableLookupPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource InputBg}"
                BorderBrush="{DynamicResource InputBorder}" BorderThickness="1" CornerRadius="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtSearch" Grid.Column="0"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{DynamicResource TextSecondary}"
                         CaretBrush="{DynamicResource TextSecondary}"
                         Padding="8,6" FontSize="13"
                         KeyDown="TxtSearch_KeyDown"
                         TextChanged="TxtSearch_TextChanged"
                         GotFocus="TxtSearch_GotFocus"
                         Style="{x:Null}"/>
                <Button x:Name="BtnToggle" Grid.Column="1" Content="&#x25BC;"
                        Click="BtnToggle_Click" Padding="6,4" Margin="0"
                        Background="Transparent" Foreground="{DynamicResource TextMuted}"
                        BorderThickness="0" FontSize="10" Cursor="Hand"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Border x:Name="ResultsPanel" Grid.Row="1"
                Background="{DynamicResource CardBg}"
                BorderBrush="{DynamicResource InputBorder}"
                BorderThickness="1" CornerRadius="0,0,4,4"
                MaxHeight="250" Visibility="Collapsed" Margin="0,-1,0,0">
            <ScrollViewer x:Name="ResultsScroll" VerticalScrollBarVisibility="Auto"
                          ScrollChanged="ResultsScroll_ScrollChanged">
                <StackPanel x:Name="ResultsList"/>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
