<UserControl
    x:Class="VTE.WPF.Views.Pages.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <!-- ListBox Item Style -->
        <Style x:Key="ReportListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#313244"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#252538"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBlue}"/>
            <Setter Property="Foreground" Value="{DynamicResource PageBg}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#a6c9ff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="ReportDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource CardBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GridBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="{DynamicResource GridRowBg}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource GridRowAltBg}"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource GridBorder}"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="True"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <!-- DataGrid Column Header Style -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource GridHeaderBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GridBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#45475a"/>
                    <Setter Property="Foreground" Value="#cdd6f4"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Row Style -->
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#45475a"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DatePicker Style -->
        <Style x:Key="DarkDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="{DynamicResource InputBg}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Width" Value="140"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource PageBg}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Report Types List -->
        <Border Grid.Column="0" Background="{DynamicResource CardBg}" CornerRadius="8" Margin="0,0,12,0">
            <DockPanel>
                <TextBlock x:Name="TxtReportTypes" DockPanel.Dock="Top" Text="Report Types" FontSize="15" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextSecondary}" Margin="12,12,12,8"/>
                <ListBox x:Name="ReportTypeList" Background="Transparent" BorderThickness="0"
                         ItemContainerStyle="{StaticResource ReportListBoxItem}"
                         SelectionChanged="OnReportTypeSelected"/>
            </DockPanel>
        </Border>

        <!-- Right: Report Content -->
        <DockPanel Grid.Column="1">
            <!-- Report Title -->
            <TextBlock DockPanel.Dock="Top" x:Name="ReportTitle" Text="Select a report" FontSize="20"
                       FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12"/>

            <!-- Date Range Filter -->
            <StackPanel DockPanel.Dock="Top" x:Name="DateFilterPanel" Orientation="Horizontal" Margin="0,0,0,12"
                        Visibility="Collapsed">
                <TextBlock x:Name="TxtFrom" Text="From" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="13"/>
                <DatePicker x:Name="DateFrom" Style="{StaticResource DarkDatePicker}"/>
                <TextBlock x:Name="TxtTo" Text="To" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="16,0,8,0" FontSize="13"/>
                <DatePicker x:Name="DateTo" Style="{StaticResource DarkDatePicker}"/>
                <Button x:Name="BtnGenerate" Content="Generate" Style="{StaticResource ActionBtn}" Margin="16,0,0,0"
                        Click="OnGenerate"/>
            </StackPanel>

            <!-- Search Box -->
            <Grid x:Name="SearchPanel" DockPanel.Dock="Top" Margin="0,8,0,8" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtReportSearch" Margin="0,0,8,0" KeyDown="ReportSearch_KeyDown"/>
                <Button x:Name="BtnReportSearch" Grid.Column="1" Content="Пребарај" Click="ReportSearch_Click"
                        Background="{DynamicResource SurfaceBg}" Foreground="{DynamicResource TextSecondary}"
                        BorderThickness="0" Padding="12,6"/>
            </Grid>

            <!-- Summary -->
            <TextBlock DockPanel.Dock="Bottom" x:Name="SummaryText" Foreground="{DynamicResource TextMuted}" FontSize="13"
                       Margin="0,8,0,0"/>

            <!-- Pagination -->
            <Border x:Name="PaginationPanel" DockPanel.Dock="Bottom" Background="{DynamicResource CardBg}" Padding="12,8" Margin="0,8,0,0" CornerRadius="6" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TxtReportRecordCount" Grid.Column="0" Foreground="{DynamicResource TextMuted}" FontSize="12" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button x:Name="BtnReportPrev" Content="&lt; Prev" Click="ReportPrev_Click" Padding="8,4" Margin="0,0,4,0"
                                Background="{DynamicResource SurfaceBg}" Foreground="{DynamicResource TextSecondary}" BorderThickness="0" FontSize="12"/>
                        <TextBlock x:Name="TxtReportPageInfo" VerticalAlignment="Center" Foreground="{DynamicResource TextMuted}" FontSize="12" Margin="8,0"/>
                        <Button x:Name="BtnReportNext" Content="Next &gt;" Click="ReportNext_Click" Padding="8,4" Margin="4,0,0,0"
                                Background="{DynamicResource SurfaceBg}" Foreground="{DynamicResource TextSecondary}" BorderThickness="0" FontSize="12"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Results Grid -->
            <DataGrid x:Name="ReportGrid" Style="{StaticResource ReportDataGrid}" AutoGeneratedColumns="ReportGrid_AutoGenerated"/>
        </DockPanel>
    </Grid>
</UserControl>
