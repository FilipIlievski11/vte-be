<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Window / Page backgrounds -->
    <SolidColorBrush x:Key="WindowBg" Color="#12121e"/>
    <SolidColorBrush x:Key="PageBg" Color="#13131e"/>
    <SolidColorBrush x:Key="SidebarBg" Color="#0f0f1a"/>
    <SolidColorBrush x:Key="SidebarBgEnd" Color="#1a1a2e"/>
    <SolidColorBrush x:Key="StatusBarBg" Color="#11111b"/>

    <!-- Surface colors -->
    <SolidColorBrush x:Key="CardBg" Color="#1a1a2a"/>
    <SolidColorBrush x:Key="CardBorder" Color="#2a2a3d"/>
    <SolidColorBrush x:Key="CardBgHover" Color="#252538"/>
    <SolidColorBrush x:Key="SurfaceBg" Color="#313244"/>
    <SolidColorBrush x:Key="InputBg" Color="#313244"/>
    <SolidColorBrush x:Key="InputBorder" Color="#45475a"/>

    <!-- Activity row -->
    <SolidColorBrush x:Key="ActivityRowBg" Color="#252538"/>

    <!-- Text colors -->
    <SolidColorBrush x:Key="TextPrimary" Color="White"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#cdd6f4"/>
    <SolidColorBrush x:Key="TextMuted" Color="#a6adc8"/>
    <SolidColorBrush x:Key="TextDimmed" Color="#6c7086"/>
    <SolidColorBrush x:Key="TextSubtle" Color="#585b70"/>
    <SolidColorBrush x:Key="StatLabel" Color="#6c7086"/>
    <SolidColorBrush x:Key="StatSubtext" Color="#7a7f96"/>

    <!-- Accent colors -->
    <SolidColorBrush x:Key="AccentPrimary" Color="#667eea"/>
    <SolidColorBrush x:Key="AccentSecondary" Color="#764ba2"/>
    <SolidColorBrush x:Key="AccentBlue" Color="#89b4fa"/>
    <SolidColorBrush x:Key="AccentGreen" Color="#a6e3a1"/>
    <SolidColorBrush x:Key="AccentYellow" Color="#f9e2af"/>
    <SolidColorBrush x:Key="AccentRed" Color="#f38ba8"/>
    <SolidColorBrush x:Key="AccentPurple" Color="#cba6f7"/>
    <SolidColorBrush x:Key="AccentTeal" Color="#06b6d4"/>

    <!-- Grid colors -->
    <SolidColorBrush x:Key="GridHeaderBg" Color="#313244"/>
    <SolidColorBrush x:Key="GridRowBg" Color="#1e1e2e"/>
    <SolidColorBrush x:Key="GridRowAltBg" Color="#252538"/>
    <SolidColorBrush x:Key="GridBorder" Color="#45475a"/>

    <!-- Nav -->
    <SolidColorBrush x:Key="NavHoverBg" Color="#252538"/>
    <SolidColorBrush x:Key="NavActiveBg" Color="#313244"/>
    <SolidColorBrush x:Key="NavText" Color="#b0b4cc"/>
    <SolidColorBrush x:Key="NavTextHover" Color="White"/>

    <!-- Button -->
    <SolidColorBrush x:Key="BtnDangerBg" Color="#f38ba8"/>
    <SolidColorBrush x:Key="BtnSecondaryBg" Color="#45475a"/>

    <!-- Stat card gradients (as solid fallbacks) -->
    <SolidColorBrush x:Key="StatBlue" Color="#1e3a5f"/>
    <SolidColorBrush x:Key="StatTeal" Color="#1e3a4f"/>
    <SolidColorBrush x:Key="StatGreen" Color="#1e4f3a"/>
    <SolidColorBrush x:Key="StatAmber" Color="#4f3a1e"/>

    <!-- System color overrides for dark theme (controls ComboBox, DatePicker, etc) -->
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#313244"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#cdd6f4"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#45475a"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#313244"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#cdd6f4"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ActiveBorderBrushKey}" Color="#45475a"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveBorderBrushKey}" Color="#45475a"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#313244"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="#cdd6f4"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ScrollBarBrushKey}" Color="#313244"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoBrushKey}" Color="#313244"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.InfoTextBrushKey}" Color="#cdd6f4"/>

    <!-- ComboBox full custom template for dark theme -->
    <ControlTemplate x:Key="DarkComboToggle" TargetType="ToggleButton">
        <Border x:Name="bd" Background="#313244" BorderBrush="#45475a" BorderThickness="1" CornerRadius="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="18"/>
                </Grid.ColumnDefinitions>
                <ContentPresenter Grid.Column="0"/>
                <Path Grid.Column="1" Data="M0,0 L4,4 8,0" Fill="#a6adc8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#585b70"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#89b4fa"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="MaxDropDownHeight" Value="300"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel VirtualizationMode="Recycling"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" Template="{StaticResource DarkComboToggle}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False" ClickMode="Press"/>
                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="8,4,24,4" VerticalAlignment="Center" HorizontalAlignment="Left"
                                          TextElement.Foreground="#cdd6f4"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"
                                 Background="Transparent" Foreground="#cdd6f4" CaretBrush="#cdd6f4" BorderThickness="0"
                                 HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                 Margin="6,2,22,2" Focusable="True"
                                 Style="{x:Null}"/>
                        <Popup x:Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Background="#313244" BorderBrush="#45475a" BorderThickness="1" CornerRadius="3">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <!-- Global TextBox styling -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#313244"/>
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="CaretBrush" Value="#cdd6f4"/>
        <Setter Property="BorderBrush" Value="#45475a"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="DatePicker">
        <Setter Property="Background" Value="#313244"/>
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="BorderBrush" Value="#45475a"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="DatePickerTextBox">
        <Setter Property="Background" Value="#313244"/>
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="CaretBrush" Value="#cdd6f4"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="2"/>
    </Style>
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="#313244"/>
        <Setter Property="Foreground" Value="#cdd6f4"/>
        <Setter Property="CaretBrush" Value="#cdd6f4"/>
        <Setter Property="BorderBrush" Value="#45475a"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
</ResourceDictionary>
