<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Window / Page backgrounds — light blue like old VTE app -->
    <SolidColorBrush x:Key="WindowBg" Color="#dce6f0"/>
    <SolidColorBrush x:Key="PageBg" Color="#e8eff7"/>
    <SolidColorBrush x:Key="SidebarBg" Color="#d4dfe9"/>
    <SolidColorBrush x:Key="SidebarBgEnd" Color="#dce6f0"/>
    <SolidColorBrush x:Key="StatusBarBg" Color="#c8d6e5"/>

    <!-- Surface colors -->
    <SolidColorBrush x:Key="CardBg" Color="#ffffff"/>
    <SolidColorBrush x:Key="CardBorder" Color="#b8c9dc"/>
    <SolidColorBrush x:Key="CardBgHover" Color="#e8eff7"/>
    <SolidColorBrush x:Key="SurfaceBg" Color="#d4dfe9"/>
    <SolidColorBrush x:Key="InputBg" Color="#ffffff"/>
    <SolidColorBrush x:Key="InputBorder" Color="#a8bdd0"/>

    <!-- Activity row -->
    <SolidColorBrush x:Key="ActivityRowBg" Color="#e8eff7"/>

    <!-- Text colors — darker for readability -->
    <SolidColorBrush x:Key="TextPrimary" Color="#0a1628"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#1a2744"/>
    <SolidColorBrush x:Key="TextMuted" Color="#2c4060"/>
    <SolidColorBrush x:Key="TextDimmed" Color="#506882"/>
    <SolidColorBrush x:Key="TextSubtle" Color="#506882"/>
    <SolidColorBrush x:Key="StatLabel" Color="#2c4060"/>
    <SolidColorBrush x:Key="StatSubtext" Color="#506882"/>

    <!-- Accent colors — blue links like old app -->
    <SolidColorBrush x:Key="AccentPrimary" Color="#1a4d8f"/>
    <SolidColorBrush x:Key="AccentSecondary" Color="#2e6ab5"/>
    <SolidColorBrush x:Key="AccentBlue" Color="#1a6dc5"/>
    <SolidColorBrush x:Key="AccentGreen" Color="#2e8b57"/>
    <SolidColorBrush x:Key="AccentYellow" Color="#d4a017"/>
    <SolidColorBrush x:Key="AccentRed" Color="#c0392b"/>
    <SolidColorBrush x:Key="AccentPurple" Color="#6c3483"/>
    <SolidColorBrush x:Key="AccentTeal" Color="#1a8a8a"/>

    <!-- Grid colors — matching old app's grid -->
    <SolidColorBrush x:Key="GridHeaderBg" Color="#c8d6e5"/>
    <SolidColorBrush x:Key="GridRowBg" Color="#ffffff"/>
    <SolidColorBrush x:Key="GridRowAltBg" Color="#edf2f7"/>
    <SolidColorBrush x:Key="GridBorder" Color="#b8c9dc"/>

    <!-- Nav — dark navy text for readability -->
    <SolidColorBrush x:Key="NavHoverBg" Color="#b8c9dc"/>
    <SolidColorBrush x:Key="NavActiveBg" Color="#a8bdd0"/>
    <SolidColorBrush x:Key="NavText" Color="#0a1a30"/>
    <SolidColorBrush x:Key="NavTextHover" Color="#000d1a"/>

    <!-- Button -->
    <SolidColorBrush x:Key="BtnDangerBg" Color="#c0392b"/>
    <SolidColorBrush x:Key="BtnSecondaryBg" Color="#c8d6e5"/>

    <!-- Stat cards -->
    <SolidColorBrush x:Key="StatBlue" Color="#dce8f5"/>
    <SolidColorBrush x:Key="StatTeal" Color="#d5eef0"/>
    <SolidColorBrush x:Key="StatGreen" Color="#d5f0dc"/>
    <SolidColorBrush x:Key="StatAmber" Color="#fffbeb"/>

    <!-- System color overrides for light theme -->
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#ffffff"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#2c3e5a"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#b8c9dc"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#1a2744"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#ffffff"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#2c3e5a"/>

    <!-- ComboBox full custom template for light theme -->
    <ControlTemplate x:Key="DarkComboToggle" TargetType="ToggleButton">
        <Border x:Name="bd" Background="#ffffff" BorderBrush="#cbd5e1" BorderThickness="1" CornerRadius="3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="18"/>
                </Grid.ColumnDefinitions>
                <ContentPresenter Grid.Column="0"/>
                <Path Grid.Column="1" Data="M0,0 L4,4 8,0" Fill="#64748b" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#94a3b8"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="bd" Property="BorderBrush" Value="#4f46e5"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="ComboBox">
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="MaxDropDownHeight" Value="300"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel VirtualizationMode="Recycling"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" Template="{StaticResource DarkComboToggle}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False" ClickMode="Press"/>
                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="8,4,24,4" VerticalAlignment="Center" HorizontalAlignment="Left"
                                          TextElement.Foreground="#1e293b"/>
                        <TextBox x:Name="PART_EditableTextBox"
                                 Visibility="Hidden" IsReadOnly="{TemplateBinding IsReadOnly}"
                                 Background="Transparent" Foreground="#1e293b" CaretBrush="#1e293b" BorderThickness="0"
                                 HorizontalAlignment="Stretch" VerticalAlignment="Center"
                                 Margin="6,2,22,2" Focusable="True"
                                 Style="{x:Null}"/>
                        <Popup x:Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border Background="#ffffff" BorderBrush="#cbd5e1" BorderThickness="1" CornerRadius="3">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <!-- Global TextBox styling -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="#ffffff"/>
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="CaretBrush" Value="#1e293b"/>
        <Setter Property="BorderBrush" Value="#cbd5e1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="DatePicker">
        <Setter Property="Background" Value="#ffffff"/>
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="BorderBrush" Value="#cbd5e1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="DatePickerTextBox">
        <Setter Property="Background" Value="#ffffff"/>
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="CaretBrush" Value="#1e293b"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="2"/>
    </Style>
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="#ffffff"/>
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="CaretBrush" Value="#1e293b"/>
        <Setter Property="BorderBrush" Value="#cbd5e1"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
</ResourceDictionary>
