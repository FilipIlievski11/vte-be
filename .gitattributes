# Shell скрипти одат на Linux сервер — секогаш LF, инаку bash се дави со \r
# (фатено 25.09.2026: деплојот падна затоа што Windows checkout-от на новото
# репо материјализираше CRLF; trunk-фајловите беа LF на диск).
*.sh text eol=lf
